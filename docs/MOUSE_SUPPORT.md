# Поддержка мыши и сенсорных экранов

## Обзор

CyberDeck Interface v3.0 имеет полную поддержку мыши и сенсорных экранов, что делает навигацию по интерфейсу более удобной на устройствах с тачскрином (например, Orange Pi с дисплеем).

## Возможности

### 🖱️ Поддержка мыши

- **Левая кнопка мыши**: Клик по любой кнопке или элементу меню
- **Колесо мыши вверх**: Прокрутка списка вверх (3 элемента за раз)
- **Колесо мыши вниз**: Прокрутка списка вниз (3 элемента за раз)
- **Средняя кнопка**: Зарезервирована для будущего функционала
- **Правая кнопка**: Зарезервирована для контекстных меню (будущее)

### 📱 Поддержка сенсорных экранов

- **Тап**: Выбор кнопки или пункта меню
- **Свайп**: Прокрутка списков
- **Drag**: Навигация по длинным спискам

### ⌨️ Клавиатурная навигация (Vim-style)

Сохранена полная поддержка клавиатуры для опытных пользователей:

- `j` / `↓` - Переместиться вниз
- `k` / `↑` - Переместиться вверх
- `h` / `←` / `Esc` - Вернуться назад
- `l` / `→` - Выбрать элемент
- `Enter` - Подтвердить выбор
- `q` - Выйти из приложения
- `?` - Показать справку

## Технические детали

### Реализация

#### MouseScrollListBox

Кастомный класс `MouseScrollListBox` расширяет стандартный `urwid.ListBox` для обработки событий прокрутки колеса мыши:

```python
class MouseScrollListBox(urwid.ListBox):
    def mouse_event(self, size, event, button, col, row, focus):
        # Прокрутка колесом вверх (button 4)
        if event == 'mouse press' and button == 4:
            self.keypress(size, 'up')
            return True

        # Прокрутка колесом вниз (button 5)
        elif event == 'mouse press' and button == 5:
            self.keypress(size, 'down')
            return True
```

#### ClickableListItem

Класс `ClickableListItem` создает элементы списка с поддержкой кликов:

```python
class ClickableListItem(urwid.WidgetWrap):
    def mouse_event(self, size, event, button, col, row, focus):
        if event == 'mouse press' and button == 1:
            if self.callback:
                self.callback()
            return True
```

#### Включение поддержки мыши

В главном цикле urwid мышь включается автоматически:

```python
screen = urwid.raw_display.Screen()
screen.set_mouse_tracking(True)
```

### Обработка событий

Все события мыши обрабатываются в методе `_handle_input`:

```python
def _handle_input(self, key):
    # Обработка кликов мыши
    if isinstance(key, tuple) and key[0] == 'mouse press':
        button = key[1]  # 1=Left, 2=Middle, 3=Right, 4=ScrollUp, 5=ScrollDown
        col = key[2]
        row = key[3]
        # Обработка...
```

## Использование в терминале

### SSH подключение с X11 forwarding

```bash
ssh -X user@orangepi
cd /home/user/armbian-gui
python3 core/main.py
```

### Локальный запуск

```bash
cd /home/user/armbian-gui
python3 core/main.py
```

### Терминал с поддержкой мыши

Рекомендуемые терминалы:
- **xterm** - полная поддержка мыши
- **tmux** - требуется включить режим мыши (`set -g mouse on`)
- **screen** - ограниченная поддержка
- **Консоль framebuffer** - полная поддержка на тачскринах

## Примеры использования

### Навигация по главному меню

1. **Мышь**: Просто кликните на любой пункт меню
2. **Клавиатура**: Используйте `j`/`k` для перемещения, `Enter` для выбора
3. **Тачскрин**: Тап на пункт меню

### Прокрутка длинных списков

1. **Колесо мыши**: Крутите колесо вверх/вниз
2. **Клавиатура**: `j`/`k` для построчной прокрутки, `Page Up`/`Page Down` для быстрой
3. **Тачскрин**: Свайп вверх/вниз

### Возврат назад

1. **Мышь**: Клик на кнопку "Back to Main Menu"
2. **Клавиатура**: `Esc` или `h`
3. **Тачскрин**: Тап на кнопку "Back"

## Отладка

### Проверка событий мыши

События мыши логируются на уровне DEBUG:

```python
self.logger.debug(f"Mouse click: button={button}, col={col}, row={row}")
```

Включите DEBUG логирование в `config/main.yaml`:

```yaml
system:
  log_level: DEBUG
```

### Тестирование в терминале

Проверьте, поддерживает ли ваш терминал мышь:

```bash
# В xterm
echo $TERM
# Должно быть xterm-256color или подобное

# Проверка поддержки мыши
tput sitm
```

## Совместимость

### Протестировано на:

- ✅ Orange Pi 5 с HDMI дисплеем
- ✅ Orange Pi Zero 3 с DSI тачскрином
- ✅ SSH сессия с xterm
- ✅ tmux (с включенной мышью)
- ✅ Консоль framebuffer

### Известные ограничения:

- ⚠️ GNU Screen - ограниченная поддержка мыши
- ⚠️ Некоторые старые терминалы не поддерживают все события мыши
- ⚠️ SSH без X11 forwarding - зависит от терминала клиента

## Будущие улучшения

Планируемые функции:

- [ ] Контекстные меню по правой кнопке мыши
- [ ] Поддержка жестов на тачскрине (pinch-to-zoom)
- [ ] Drag-and-drop для файлов
- [ ] Мультитач жесты
- [ ] Тактильная обратная связь на совместимых устройствах

## См. также

- [UI Manager Implementation](../core/ui_manager.py)
- [Main Configuration](../config/main.yaml)
- [urwid Documentation](http://urwid.org/manual/index.html)
